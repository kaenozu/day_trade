feat(ml): Add framework for advanced ML models

- Introduces a model management framework in `src/day_trade/analysis/models.py` with a base `TradingModel` class.
- Adds a placeholder `LightGBMModel`.
- Refactors the existing rule-based logic into a `RuleBasedModel`.
- The `create_trading_recommendation_pl` function can now select which model to use for prediction.
- The recommendation service is updated to use the new LightGBM model by default.
- Adds `lightgbm` to dependencies.

This addresses the "Introduction of Advanced Machine Learning Models" part of issue #939.